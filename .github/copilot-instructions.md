The only rules you need to follow when writing code are:
1) Always disable pycache
2) Any tests are always written in /tests
3) Never create log files, always use the database for saving and retrieving data.