# MINIMAL REQUIREMENTS FOR CONTINUOUS LEARNING SYSTEM
# Only the absolutely essential dependencies needed for the enhanced continuous learning loop

# Core dependencies - REQUIRED
numpy>=1.24.0              # Mathematical operations, array handling for agent states
asyncio                    # Built-in - Async operations for SWARM mode concurrent training

# ARC-AGI-3 API Integration - REQUIRED  
aiohttp>=3.8.0            # HTTP client for ARC-AGI-3 API calls
requests>=2.28.0          # Alternative HTTP client (backup)
python-dotenv>=1.0.0      # Environment variable management for API keys

# Machine Learning Core - REQUIRED
torch>=2.0.0              # Neural networks for predictive core, sleep system, memory operations
scipy>=1.10.0             # Scientific computing for salience calculations

# Data & Configuration - REQUIRED
pyyaml>=6.0               # Configuration file handling
pydantic>=2.0.0           # Data validation for API responses

# Development - OPTIONAL (for debugging/testing only)
pytest>=7.4.0             # Testing framework (only if running tests)
